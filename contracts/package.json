{
  "name": "bounty-board-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:revive": "hardhat run scripts/deploy.ts --network reviveTestnet",
    "deploy:revive-local": "hardhat run scripts/deploy.ts --network reviveLocal",
    "smoke:revive": "hardhat run scripts/smoke.ts --network reviveTestnet",
    "smoke:revive-local": "hardhat run scripts/smoke.ts --network reviveLocal"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0"
  }
}
